package com.barbasdev.base.redux

/**
 * A redux state - duh!
 *
 * Children classes should model the information required to update the state of a feature.
 * That could be the UI, the data stored in a local database, etc.
 */
abstract class State
